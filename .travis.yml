language: cpp

services:
  - docker

before_install:
  - docker pull matgla/esp8266
  #- docker run -d -p 127.0.0.1:4567 matgla/esp8266 /bin/sh -c "cd ~; bundler exec foreman start;"
  - docker ps -a

script:
  - docker run -v ($pwd):/mnt/repo -it matgla/esp8266 /bin/sh -c "cd /mnt/repo && sh run_tests.sh"
